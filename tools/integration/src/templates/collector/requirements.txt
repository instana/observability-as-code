# Python dependencies for {{COLLECTOR_NAME}} collector
#
# NOTE: The following packages are already provided by the base image.

# OpenTelemetry Core & Exporters
# opentelemetry-api==1.42.1
# opentelemetry-sdk==1.42.1
# opentelemetry-exporter-otlp-proto-grpc==1.42.1
# opentelemetry-exporter-otlp-proto-http==1.42.1

# Extension Base Library
# extension-base==1.0.0

# ---------------------------------------------------------------------
# Add your additional required packages below.
# ---------------------------------------------------------------------

# HTTP clients
# httpx>=0.28.1           # supports both sync and async (recommended)
# requests>=2.34.2        # synchronous only

# Database connectors
# psycopg[binary]>=3.3.4  # PostgreSQL (binary: faster, no build tools needed)
# pymysql[rsa]>=1.2.0     # MySQL — [rsa] required for caching_sha2_password auth
# redis>=8.1.0            # Redis
